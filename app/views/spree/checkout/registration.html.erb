<div class="container">
  <%= render :partial => 'spree/shared/error_messages', :locals => { :target => @user } %>
  <h1><%= Spree.t(:registration) %></h1>
  <div id="registration" class="row form-content" data-hook>
    <div id="account">
      <%= render :partial => 'new_user' %>
    </div>
    <% if Spree::Config[:allow_guest_checkout] %>
      <div class="col-md-6">
        <div class="">
          <div class="form-title"><%= Spree.t(:guest_user_account) %></div>
          <div id="guest_checkout" class="" data-hook>
            <% if flash[:registration_error] %>
              <div class='flash error'><%= flash[:registration_error] %></div>
            <% end %>
            <%= form_for @order, :url => update_checkout_registration_path, :method => :put, :html => { :id => 'checkout_form_registration' } do |f| %>
              <p>
                <%= f.email_field :email, :class => 'form-control title', :placeholder => Spree.t(:email) %>
              </p>
              <p><%= f.submit Spree.t(:continue), :class => 'button' %></p>
            <% end %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
